#base * altura / 2

base = float(input('Informe a base do triângulo: '))
altura = float(input('Informe a altura: '))

area = (base * altura) / 2

print(f'A área do triangulo é de {area:.2f}Cm')
